# RPA Fax Classification Agent - Python Dependencies
# Install: pip install -r requirements.txt
# Then:    playwright install chromium

# Secrets loading from .env file
python-dotenv

# Browser automation (ECW navigation)
playwright

# Camoufox stealth browser (bypasses bot detection / Cloudflare)
# Falls back to standard Playwright if not installed
# Install separately: pip install camoufox
camoufox

# HTTP client (PDF download + Vertex AI API calls)
httpx

# Excel audit log
openpyxl

# PDF Pipeline (local, HIPAA safe)
# PDF reading and splitting
pypdf

# PDF to image conversion for local OCR (fitz module)
PyMuPDF

# Google Vertex AI (Gemini classifier - BAA covered)
google-auth
google-auth-httplib2
google-api-python-client

# NOT needed:
# anthropic  - classifier.py (Claude) is a LEGACY file, not used
# boto3      - AWS Bedrock not used in this pipeline
